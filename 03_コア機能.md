# 03 コア機能

> Claude Code の組み込みツール、CLI、Git連携、ドキュメント生成を体系的にまとめたルール集。
> 「基礎スキル判定」の参照データとして使用する。

---

## A. ファイル操作ツール

### A-1. Read

| # | ルール | 補足 |
|---|--------|------|
| R-001 | `file_path` は絶対パスで指定する | 相対パスは不可 |
| R-002 | デフォルトで最大2000行を読み取る | 大きいファイルは `offset` / `limit` で範囲指定 |
| R-003 | 結果は `cat -n` 形式（行番号付き、1始まり）で返る | |
| R-004 | 画像ファイル（PNG, JPG等）を読み取ると視覚的にコンテンツが提示される | マルチモーダルLLMとして機能 |
| R-005 | PDFは10ページ超の場合 `pages` パラメータ必須 | 最大20ページ/リクエスト。省略すると失敗 |
| R-006 | Jupyter Notebook（.ipynb）は全セルと出力を統合して返す | コード・テキスト・可視化を含む |
| R-007 | ディレクトリは読み取り不可 | `ls` コマンドを使用する |
| R-008 | スクリーンショットのパスが提供された場合は常にReadで閲覧する | 一時ファイルパスにも対応 |

**情報源:** システムプロンプト（Read tool定義）

### A-2. Write

| # | ルール | 補足 |
|---|--------|------|
| R-009 | 既存ファイルは事前にReadで読み取ってからでないとエラー | 未読の既存ファイルへの書き込みは失敗する |
| R-010 | 既存ファイルの変更にはEditツールを優先する | Writeは新規作成 or 完全書き換え専用 |
| R-011 | ドキュメントファイル（*.md）やREADMEは明示的要求がない限り作成しない | |
| R-012 | 絵文字はユーザーが明示的に要求しない限りファイルに書かない | |

**情報源:** システムプロンプト（Write tool定義）

### A-3. Edit

| # | ルール | 補足 |
|---|--------|------|
| R-013 | 会話中に少なくとも1回Readしてからでないとエラー | |
| R-014 | `old_string` がファイル内で一意でないと編集失敗 | 周辺コンテキストを増やして一意にするか `replace_all: true` を使う |
| R-015 | Readの行番号プレフィックス部分を `old_string` に含めない | 行番号の後のインデントはそのまま保持 |
| R-016 | `replace_all` はファイル全体の変数名リネーム等に使う | |
| R-017 | 既存ファイルの編集を常に優先し、新規ファイル作成は必要最小限にする | |

**情報源:** システムプロンプト（Edit tool定義）

### A-4. Glob

| # | ルール | 補足 |
|---|--------|------|
| R-018 | ファイル名パターンでの検索には `find` ではなくGlobを使う | 例: `**/*.js`, `src/**/*.ts` |
| R-019 | 結果は変更日時順にソートされる | |
| R-020 | `path` パラメータに `"undefined"` や `"null"` を入力しない | 省略すればカレントディレクトリが使われる |

**情報源:** システムプロンプト（Glob tool定義）

### A-5. Grep

| # | ルール | 補足 |
|---|--------|------|
| R-021 | ファイル内容の検索には常にGrepを使う。Bashで `grep` や `rg` を実行しない | |
| R-022 | ripgrepの正規表現構文を使う | リテラルブレースはエスケープ: `interface\{\}` |
| R-023 | 複数行マッチには `multiline: true` を指定する | 例: `struct \{[\s\S]*?field` |
| R-024 | 標準ファイルタイプの絞り込みは `type` パラメータが `glob` より効率的 | 例: `type: "js"` |
| R-025 | `output_mode` のデフォルトは `files_with_matches` | `content` でマッチ行表示、`count` でカウント |
| R-026 | `head_limit` のデフォルトは250。`0`（無制限）はコンテキスト浪費に注意 | |

**情報源:** システムプロンプト（Grep tool定義）

### A-6. ツール使い分け原則

| # | ルール | 補足 |
|---|--------|------|
| R-027 | ファイル名検索 → Glob（`find` / `ls` の代わり） | |
| R-028 | ファイル内容検索 → Grep（`grep` / `rg` の代わり） | |
| R-029 | ファイル読み取り → Read（`cat` / `head` / `tail` の代わり） | |
| R-030 | ファイル編集 → Edit（`sed` / `awk` の代わり） | |
| R-031 | ファイル書き込み → Write（`echo >` の代わり） | |
| R-032 | agentic search（Glob + Grep）はRAGより優れる | ベクトルDBはコードの同期ずれとパーミッション問題があるため不採用 |

**情報源:** システムプロンプト、https://github.com/shanraisshan/claude-code-best-practice

---

## B. Bash Tool

| # | ルール | 補足 |
|---|--------|------|
| R-033 | 作業ディレクトリはコマンド間で保持されるが、シェル状態は保持されない | |
| R-034 | デフォルトタイムアウトは120,000ms（2分）、最大600,000ms（10分） | |
| R-035 | スペースを含むパスはダブルクォートで囲む | |
| R-036 | 独立したコマンドは並列で複数のBash呼び出しにする | |
| R-037 | 依存関係のあるコマンドは `&&` でチェーンする | |
| R-038 | 前のコマンド失敗を気にしない場合は `;` で区切る | |
| R-039 | 改行でコマンドを区切らない | クォート文字列内の改行は可 |
| R-040 | 不要な `sleep` コマンドは避ける | `run_in_background` で非同期実行し、後でReadで結果取得 |
| R-041 | `find`, `grep`, `cat`, `head`, `tail`, `sed`, `awk`, `echo` は専用ツールを優先する | 明示的に指示された場合 or 専用ツールで不可能な場合のみBashで実行 |

**情報源:** システムプロンプト（Bash tool定義）

---

## C. CLIリファレンス

### C-1. コアコマンド

| # | ルール | コマンド例 |
|---|--------|----------|
| R-042 | インタラクティブモード起動 | `claude` |
| R-043 | プロンプト付き起動 | `claude "質問"` |
| R-044 | ヘッドレス（非インタラクティブ）モード | `claude -p "質問"` |
| R-045 | 最後の会話を継続 | `claude -c` |
| R-046 | セッション名で再開 | `claude -r "名前"` |

### C-2. 主要フラグ

| # | ルール | フラグ |
|---|--------|--------|
| R-047 | モデル指定 | `--model` |
| R-048 | Git worktreeで隔離ブランチ | `-w` / `--worktree` |
| R-049 | ツールの事前承認 | `--allowedTools` 例: `["Read", "Grep", "Glob"]` |
| R-050 | 出力形式指定 | `--output-format json` / `stream` |
| R-051 | 構造化出力スキーマ | `--json-schema` |
| R-052 | ターン数制限 | `--max-turns` |
| R-053 | コスト上限 | `--max-budget-usd` 例: `5` |
| R-054 | 推論レベル | `--effort low` / `med` / `high` / `max` |
| R-055 | 最小ヘッドレスモード（hooks/LSP/plugins無し） | `--bare` |
| R-056 | 追加ワーキングディレクトリ | `--add-dir` |
| R-057 | セッション名指定 | `-n` / `--name` |

### C-3. パーミッションモード

| # | ルール | モード |
|---|--------|--------|
| R-058 | 標準のパーミッションプロンプト | `default` |
| R-059 | ファイル編集を自動承認 | `acceptEdits` |
| R-060 | 読み取り専用（実行なし） | `plan` |
| R-061 | 未承認ツールは拒否（プロンプトなし） | `dontAsk` |
| R-062 | 全パーミッションチェックをスキップ | `bypassPermissions` |
| R-063 | バックグラウンド安全分類器がプロンプト注入や危険なエスカレーションをブロック | `auto` |
| R-064 | 安全な運用は `allowedTools` + `dontAsk` の組み合わせ | `bypassPermissions` より安全 |

### C-4. システムプロンプトカスタマイズ

| # | ルール | 補足 |
|---|--------|------|
| R-065 | `--system-prompt` でシステムプロンプトを完全置換 | `--system-prompt-file` とは排他 |
| R-066 | `--append-system-prompt` で既存に追加 | 組み込み機能を保持しつつ要件追加。通常はこちらを推奨 |
| R-067 | append系フラグはreplacement系と組み合わせ可能 | |

### C-5. スラッシュコマンド

| # | ルール | コマンド | 説明 |
|---|--------|---------|------|
| R-068 | コンテキスト圧縮 | `/compact [focus]` | フォーカス指定でトピック絞り込み |
| R-069 | 会話分岐 | `/branch [name]` | `/fork` エイリアス |
| R-070 | CLAUDE.md作成 | `/init` | |
| R-071 | CLAUDE.md編集 | `/memory` | |
| R-072 | MCPサーバー管理 | `/mcp` | |
| R-073 | Hook管理 | `/hooks` | |
| R-074 | プランモード | `/plan [desc]` | |
| R-075 | マルチエージェントコードレビュー | `/simplify` | 3並列エージェント |
| R-076 | 大規模並列変更 | `/batch` | 5-30 worktree |
| R-077 | サイドクエスチョン | `/btw <question>` | コンテキスト消費なし |
| R-078 | インストール診断 | `/doctor` | |
| R-079 | モデル切替 | `/model [model]` | |
| R-080 | 推論レベル設定 | `/effort [level]` | |

### C-6. キーボードショートカット

| # | ルール | ショートカット | 動作 |
|---|--------|--------------|------|
| R-081 | 入力/生成キャンセル | `Ctrl+C` | |
| R-082 | セッション終了 | `Ctrl+D` | |
| R-083 | エディタでプロンプト編集 | `Ctrl+G` | |
| R-084 | タスクをバックグラウンドに | `Ctrl+B` | |
| R-085 | 画像ペースト | `Ctrl+V` | |
| R-086 | 巻き戻し/取り消し | `Esc Esc` | |
| R-087 | パーミッションモード切替 | `Shift+Tab` | |
| R-088 | モデル切替 | `Alt+P` | |
| R-089 | 改行入力 | `\+Enter` | |

### C-7. 環境変数

| # | ルール | 変数 | 説明 |
|---|--------|------|------|
| R-090 | APIキー設定 | `ANTHROPIC_API_KEY` | |
| R-091 | デフォルトモデル設定 | `ANTHROPIC_MODEL` | |
| R-092 | 推論レベル設定 | `CLAUDE_CODE_EFFORT_LEVEL` | `low` / `med` / `high` |
| R-093 | 思考トークン無効化 | `MAX_THINKING_TOKENS=0` | |
| R-094 | 最大出力トークン設定 | `CLAUDE_CODE_MAX_OUTPUT_TOKENS` | デフォルト32K |
| R-095 | Claude Codeシェル検出 | `CLAUDECODE=1` | |

**情報源:** https://code.claude.com/docs/en/cli-reference, https://github.com/wilwaldon/claude-code-cheat-sheet

---

## D. Git連携

### D-1. コミットの安全ルール

| # | ルール | 補足 |
|---|--------|------|
| R-096 | ユーザーが明示的に要求した場合のみコミットを作成する | |
| R-097 | git configを変更しない | |
| R-098 | 破壊的コマンドはユーザーの明示的指示なしに実行しない | `push --force`, `reset --hard`, `checkout .`, `restore .`, `clean -f`, `branch -D` |
| R-099 | hookスキップ禁止 | `--no-verify`, `--no-gpg-sign` は使わない |
| R-100 | 常に新規コミットを作成する（amendしない） | pre-commitフック失敗時、amendは前のコミットを破壊するリスクがある |
| R-101 | `git add -A` / `git add .` より個別ファイル指定を優先 | `.env` 等の機密ファイルの誤コミット防止 |
| R-102 | main/masterへのforce pushは警告する | |
| R-103 | `-i` フラグ（インタラクティブ）は非対応 | `git rebase -i`, `git add -i` 等 |
| R-104 | `git rebase` に `--no-edit` は使用不可 | |

### D-2. コミットワークフロー

| # | ルール | 補足 |
|---|--------|------|
| R-105 | Step 1: `git status` + `git diff` + `git log` を並列実行 | |
| R-106 | Step 2: 変更を分析しコミットメッセージを起草 | |
| R-107 | Step 3: ステージング + コミット + `git status` 確認 | |
| R-108 | コミットメッセージはHEREDOC形式で渡す | 下記コマンド例参照 |
| R-109 | pre-commitフック失敗時は修正して新規コミットを作成 | amendしない |

**コミットメッセージ例:**
```bash
git commit -m "$(cat <<'EOF'
Add user authentication middleware

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

### D-3. PR作成ワークフロー

| # | ルール | 補足 |
|---|--------|------|
| R-110 | Step 1: `git status` + `git diff` + リモート追跡確認 + `git log` + `git diff [base]...HEAD` を並列実行 | |
| R-111 | Step 2: 全コミットを分析しPRタイトル（70文字以内）・サマリーを起草 | 最新コミットだけでなく全コミットを確認 |
| R-112 | Step 3: ブランチ作成 + push + `gh pr create` を並列実行 | |

**PR作成コマンド例:**
```bash
gh pr create --title "the pr title" --body "$(cat <<'EOF'
## Summary
<1-3 bullet points>

## Test plan
[Bulleted markdown checklist of TODOs for testing the pull request...]

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

### D-4. Git / PRベストプラクティス

| # | ルール | 補足 |
|---|--------|------|
| R-113 | PRは小さく保つ | p50で118行 |
| R-114 | 常にsquash merge | 1機能=1コミット、`git revert`/`bisect` が容易 |
| R-115 | 最低1時間に1回コミット | タスク完了時すぐに |
| R-116 | PRコメント閲覧 | `gh api repos/foo/bar/pulls/123/comments` |
| R-117 | Git worktreeで並列開発 | `claude --worktree feature-name` または frontmatter `isolation: worktree` |

**情報源:** システムプロンプト、https://github.com/shanraisshan/claude-code-best-practice

---

## E. Web検索/フェッチ

| # | ルール | 補足 |
|---|--------|------|
| R-118 | WebSearchは検索クエリ最小2文字 | `allowed_domains` / `blocked_domains` でドメイン絞り込み可 |
| R-119 | WebSearch使用後は必ず「Sources:」セクションにURLを記載する | |
| R-120 | WebFetchはURLを取得しHaiku（小型モデル）で処理して回答のみ返す | 生のHTML/マークダウンは返さない |
| R-121 | WebFetchはHTTPを自動でHTTPSにアップグレードする | 15分間のキャッシュ有り |
| R-122 | 認証が必要なURL（Google Docs, Confluence, Jira, GitHub）ではWebFetchは失敗する | 専用MCPツールや `gh` CLIを使う |
| R-123 | MCPベースのWeb fetchツールが利用可能な場合はそちらを優先する | |

**情報源:** システムプロンプト（WebSearch/WebFetch tool定義）、https://mikhail.io/2025/10/claude-code-web-tools/

---

## F. マルチモーダル入力

| # | ルール | 補足 |
|---|--------|------|
| R-124 | 画像はReadツールで読み取るか `Ctrl+V` でペースト | |
| R-125 | PDFは10ページ超で `pages` パラメータ必須（最大20ページ/リクエスト） | |
| R-126 | Jupyter Notebookは全セルと出力を統合して返す | |

**情報源:** システムプロンプト（Read tool定義）

---

## G. ドキュメント生成Skills

### G-1. DOCX（Word文書）

| # | ルール | 補足 |
|---|--------|------|
| R-127 | 新規作成は `docx-js`（JavaScript）、既存編集は展開→XML編集→再パック | 読み取りは `pandoc` またはXML展開 |
| R-128 | docx-jsで改行に `\n` を使わない | 別々のParagraph要素を使う |
| R-129 | テーブル幅はDXAで指定する | `WidthType.PERCENTAGE` はGoogle Docsで破綻 |
| R-130 | `ShadingType.CLEAR` を使う | `SOLID` は黒背景になる |

### G-2. XLSX（スプレッドシート）

| # | ルール | 補足 |
|---|--------|------|
| R-131 | データ分析にはpandas、複雑なフォーマットにはopenpyxlを使う | |
| R-132 | Pythonで計算してハードコードせず、Excel数式を使う | 例: `sheet['B10'] = '=SUM(B2:B9)'` |
| R-133 | 保存後に数式再計算を実行する | `python scripts/recalc.py output.xlsx` |
| R-134 | 財務モデルの色コーディング: 青=入力、黒=数式、緑=シート間リンク、赤=外部リンク、黄背景=要注意 | |

### G-3. PPTX（プレゼンテーション）

| # | ルール | 補足 |
|---|--------|------|
| R-135 | ゼロから作成はpptxgenjs、テンプレート編集は展開→XML編集→再パック | 読み取りは `python -m markitdown` |
| R-136 | 全スライドにビジュアル要素必須（テキストのみは禁止） | |
| R-137 | タイトル下のアクセントライン禁止 | AI生成スライドの特徴として避ける |
| R-138 | QA必須: markitdownで確認 + サブエージェントで視覚QA + 最低1回の修正・検証サイクル | |

### G-4. PDF

| # | ルール | 補足 |
|---|--------|------|
| R-139 | PDFマージ/分割はpypdf、テキスト/テーブル抽出はpdfplumber、新規作成はreportlab | |
| R-140 | ReportLabでUnicode下付き/上付き文字を使わない | `<sub>` / `<super>` タグを使う |
| R-141 | OCRはpytesseract（画像変換後）を使う | |

**情報源:** https://github.com/anthropics/skills/blob/main/skills/ 各SKILL.md

---

## H. Anthropic Skills

| # | ルール | 補足 |
|---|--------|------|
| R-142 | Skillsはフォルダ構成の指示・スクリプト・リソースで、動的にロードされる | |
| R-143 | Skill定義にはfrontmatter（name, description, allowed-tools等）を使う | `context: fork` でサブエージェント実行 |
| R-144 | docx/xlsx/pptx/pdf のスキルはProprietary（オープンソースではない） | |

**情報源:** https://github.com/anthropics/skills

---

## I. 最新Claudeモデルと新APIサーフェス（2026年6月更新）

### I-1. 最新モデル一覧

| # | ルール | モデルID | 補足 |
|---|--------|---------|------|
| R-145 | 最新最高性能モデルはClaude Fable 5 | `claude-fable-5` | 1Mコンテキスト、最高精度の推論・長期エージェント作業向け |
| R-146 | Claude Mythos 5はFable 5と同一能力・同一価格・同一API仕様 | `claude-mythos-5` | Project Glasswing限定アクセス |
| R-147 | Claude Opus 4.8はデフォルト推奨モデル | `claude-opus-4-8` | 1Mコンテキスト、Adaptive Thinking対応 |
| R-148 | Fable 5 / Opus 4.7 / 4.8では `budget_tokens` が完全削除 | `thinking: {type: "adaptive"}` に移行 | `budget_tokens` 指定で400エラー |
| R-149 | Fable 5では `thinking: {type: "disabled"}` も400エラー | パラメータ自体を省略する | Opus 4.7/4.8は `disabled` は受付 |

**情報源:** https://github.com/anthropics/skills/blob/main/skills/claude-api/SKILL.md（2026-06-27更新）

### I-2. サーバーツール最新バリアント

| # | ルール | ツールタイプ | 補足 |
|---|--------|------------|------|
| R-150 | コード実行には `code_execution_20260521` を使用する | `bash_code_execution_tool_result` で結果取得 | `.content.stdout` / `.stderr` / `.return_code` |
| R-151 | Web検索の最新バリアントは `web_search_20260209` | 動的フィルタリング対応 | Opus 4.8/4.7/4.6、Sonnet 4.6で有効 |
| R-152 | Web取得の最新バリアントは `web_fetch_20260209` | 動的フィルタリング対応 | Vertex AIでは利用不可 |
| R-153 | `code_execution_20260521` 結果は `bash_code_execution_tool_result` 型 | 旧 `code_execution_tool_result` とは異なる | `response.content` を型で判別すること |

**情報源:** https://github.com/anthropics/skills/blob/main/skills/claude-api/SKILL.md（2026-06-27更新）

### I-3. Managed Agents（新APIサーフェス）

| # | ルール | 補足 |
|---|--------|------|
| R-154 | Managed AgentsはAnthropicがエージェントループとコンテナを管理する第3のAPIサーフェス | Claude API + tool use（自前ループ）とは異なる |
| R-155 | Managed Agentsは永続エージェント設定（POST /v1/agents）とセッション（sessions）で構成される | エージェントIDを作成し、以降は同IDで `sessions.create` を呼ぶ |
| R-156 | セッションでファイル操作・Bash・コード実行はAnthropicの管理コンテナ内で実行される | Bedrock / Vertex / FoundryではManaged Agentsは非対応 |

**情報源:** https://github.com/anthropics/skills/blob/main/skills/claude-api/SKILL.md（2026-06-27更新）

---

## 付録1: ツール選択フローチャート

```
タスクを受け取った
  |
  +-- ファイルを探したい？
  |     +-- ファイル名/パターンで探す → Glob (R-018)
  |     +-- ファイル内容で探す → Grep (R-021)
  |
  +-- ファイルを読みたい？
  |     +-- 画像/PDF/Notebook → Read (R-004, R-005, R-006)
  |     +-- 通常のファイル → Read (R-001)
  |     +-- ディレクトリ一覧 → Bash (`ls`)  (R-007)
  |
  +-- ファイルを変更したい？
  |     +-- 部分的な編集 → Edit (R-013)
  |     +-- 全体書き換え/新規作成 → Write (R-009, R-010)
  |
  +-- シェルコマンドを実行したい？
  |     +-- find/grep/cat/sed 等 → 専用ツール優先 (R-041)
  |     +-- それ以外 → Bash (R-033)
  |
  +-- Web情報が必要？
  |     +-- 検索したい → WebSearch (R-118)
  |     +-- 特定URLの内容を取得 → WebFetch (R-120)
  |     +-- 認証が必要なURL → 専用MCP / gh CLI (R-122)
  |
  +-- Gitオペレーション？
  |     +-- コミット → 安全ルール確認 (R-096〜R-104) → ワークフロー (R-105〜R-109)
  |     +-- PR作成 → ワークフロー (R-110〜R-112)
  |
  +-- ドキュメント生成？
        +-- Word → docx-js or XML編集 (R-127)
        +-- Excel → pandas or openpyxl (R-131)
        +-- PowerPoint → pptxgenjs or XML編集 (R-135)
        +-- PDF → pypdf / pdfplumber / reportlab (R-139)
```

---

## 付録2: よくある間違い表

| # | 間違い | 正しい対応 | 関連ルール |
|---|--------|-----------|----------|
| 1 | Bashで `grep` コマンドを実行する | Grepツールを使う | R-021 |
| 2 | Bashで `find` コマンドを実行する | Globツールを使う | R-018 |
| 3 | Bashで `cat` / `head` / `tail` を実行する | Readツールを使う | R-029 |
| 4 | Bashで `sed` / `awk` を実行する | Editツールを使う | R-030 |
| 5 | Readせずに既存ファイルをWriteする | 先にReadしてからWriteする | R-009 |
| 6 | 部分編集にWriteを使う | Editツールを使う（差分のみ送信で効率的） | R-010 |
| 7 | Editの `old_string` に行番号を含める | 行番号プレフィックスは除外する | R-015 |
| 8 | `old_string` が一意でなくEditが失敗 | 周辺コンテキストを増やすか `replace_all` を使う | R-014 |
| 9 | `file_path` に相対パスを指定する | 絶対パスを使う | R-001 |
| 10 | 大きなPDFを `pages` なしで読む | `pages` パラメータを指定する | R-005 |
| 11 | `git add -A` で全ファイルをステージング | 個別ファイル指定でステージングする | R-101 |
| 12 | pre-commitフック失敗後に `--amend` する | 修正して新規コミットを作成する | R-100 |
| 13 | ユーザーの指示なしにコミットを作成する | 明示的に要求された場合のみコミット | R-096 |
| 14 | `--no-verify` でhookをスキップする | hookの問題を調査して修正する | R-099 |
| 15 | 破壊的gitコマンドを確認なしに実行する | ユーザーの明示的指示を待つ | R-098 |
| 16 | WebFetchで認証付きURLにアクセスする | 専用MCPツールや `gh` CLIを使う | R-122 |
| 17 | docx-jsで `\n` を改行に使う | 別々のParagraph要素を使う | R-128 |
| 18 | Excel数式をPythonで計算してハードコード | `=SUM(B2:B9)` のようにExcel数式を使う | R-132 |
| 19 | PPTXでテキストのみのスライドを作成する | 全スライドにビジュアル要素を含める | R-136 |
| 20 | ReportLabでUnicode上付き/下付き文字を使う | `<sub>` / `<super>` タグを使う | R-140 |
