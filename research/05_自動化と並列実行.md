# 05. 自動化と並列実行

> 調査日: 2026-03-28
> ソース: Anthropic公式ドキュメント、GitHub各リポジトリREADME

---

## 目次

1. [Headlessモード](#1-headlessモード)
2. [GitHub Actions](#2-github-actions)
3. [スケジュール実行](#3-スケジュール実行)
4. [Agent SDK](#4-agent-sdk)
5. [並列実行](#5-並列実行)
6. [オーケストレーター](#6-オーケストレーター)
7. [マルチモデル並列・トークン予算管理](#7-マルチモデル並列トークン予算管理)

---

## 1. Headlessモード

> ソース: https://docs.anthropic.com/en/docs/claude-code/advanced

### 1.1 基本フラグ

| フラグ | 説明 |
|--------|------|
| `-p` / `--print` | 非対話（Headless）モードで実行。プロンプトを渡して結果を標準出力に返す |
| `--output-format` | 出力形式を指定: `text`（デフォルト）, `json`, `streaming-json` |
| `--session-id <id>` | セッションを指定して会話を継続 |
| `--resume` | 直前のセッションを再開 |
| `--continue` | 直前のセッションのコンテキストを引き継いで新規プロンプトを実行 |
| `--max-turns <n>` | エージェントループの最大ターン数を制限 |
| `--allowedTools` | 使用を許可するツールを指定 |
| `--disallowedTools` | 使用を禁止するツールを指定 |

### 1.2 使用例

```bash
# 基本的なHeadless実行
claude -p "このプロジェクトの構造を説明して"

# JSON形式で出力
claude -p "READMEを要約して" --output-format json

# ストリーミングJSON形式
claude -p "テスト追加して" --output-format streaming-json

# セッション継続
claude -p "前の続き" --session-id my-session-123

# ツール制限付き
claude -p "コードレビューして" --allowedTools "Read,Grep,Glob"

# ターン数制限
claude -p "バグを修正して" --max-turns 10
```

### 1.3 CI/CD環境での利用

CI/CD環境では以下の環境変数を設定して利用する:

```bash
export ANTHROPIC_API_KEY="sk-ant-..."
# 非対話モードで自動承認
claude -p "npm testを実行して結果を報告" --allowedTools "Bash(npm:*),Read"
```

### 1.4 V2 Session API

> ソース: https://github.com/anthropics/claude-agent-sdk-demos （hello-world-v2）

V1の `query()` が全メッセージのAsync Generatorだったのに対し、V2は `send()` と `stream()` を分離し、マルチターン会話を実現する。

```typescript
// セッション作成
const session = unstable_v2_createSession({ model: 'sonnet' });

// メッセージ送信 + ストリーム取得
await session.send();
for await (const event of session.stream()) {
  // イベント処理
}

// セッション再開（永続化）
const resumed = unstable_v2_resumeSession(sessionId, { model: 'sonnet' });

// ワンショット（セッション不要の単発実行）
const result = unstable_v2_prompt('質問内容', { model: 'sonnet' });
```

**V1 vs V2 の違い:**

| 項目 | V1 (`query()`) | V2 (Session API) |
|------|----------------|-------------------|
| パターン | Async Generator | send() + stream() |
| 会話 | 単発プロンプト | マルチターン対応 |
| 永続化 | なし | sessionIdで再開可能 |
| ワンショット | query()で実行 | unstable_v2_prompt() |

---

## 2. GitHub Actions

> ソース: https://docs.anthropic.com/en/docs/claude-code/github-actions
> ソース: https://github.com/anthropics/claude-code-action

### 2.1 概要

`anthropics/claude-code-action` は、GitHub PR/Issueに対してClaude Codeを統合するGitHub Action。自動モード検出により、メンション応答・コードレビュー・実装支援を自動切替する。

- **Stars**: 6.7k / **License**: MIT / **Latest**: v1.0
- 認証: Anthropic API直接 / AWS Bedrock / Google Vertex AI / Microsoft Foundry

### 2.2 クイックスタート

```bash
claude /install-github-app
```

これにより以下が自動設定される:
- GitHub Appインストール
- シークレット設定
- リポジトリセットアップ

### 2.3 主要入力パラメータ

| パラメータ | デフォルト | 説明 |
|-----------|-----------|------|
| `anthropic_api_key` | - | Anthropic APIキー |
| `claude_code_oauth_token` | - | OAuth認証トークン |
| `prompt` | - | カスタムプロンプト指示 |
| `trigger_phrase` | `@claude` | コメントトリガーフレーズ |
| `assignee_trigger` | - | ユーザー名トリガー |
| `label_trigger` | `claude` | ラベルトリガー |
| `track_progress` | - | 進捗トラッキングコメント有効化 |
| `claude_args` | - | Claude CLIへの追加引数 |
| `include_fix_links` | `true` | PRレビューの「Fix this」リンク |
| `use_sticky_comment` | `false` | 単一コメントに統合 |
| `classify_inline_comments` | `true` | インラインコメント分類 |
| `branch_prefix` | `claude/` | ブランチ名プレフィックス |
| `use_commit_signing` | - | コミット署名有効化 |
| `settings` | - | JSON設定文字列またはファイルパス |
| `additional_permissions` | - | 追加権限 |
| `allowed_bots` | - | 許可するbotリスト |
| `plugins` | - | プラグインリスト |
| `use_bedrock` | - | AWS Bedrock OIDC有効化 |
| `use_vertex` | - | Google Vertex AI OIDC有効化 |
| `anthropic_federation_rule_id` | - | Workload Identity Federation ルールID |
| `anthropic_organization_id` | - | AnthropicオーガニゼーションID（WIF認証時） |
| `anthropic_service_account_id` | - | サービスアカウントID（WIF認証時） |
| `anthropic_workspace_id` | - | ワークスペースID（WIF認証時） |
| `anthropic_oidc_audience` | `https://api.anthropic.com` | OIDCオーディエンス（WIF認証時） |

WIFを使うと静的APIキー不要で GitHub Actions OIDC トークンを Anthropic の短命アクセストークンに交換できる（`ANTHROPIC_IDENTITY_TOKEN_FILE` 経由）。`id-token: write` パーミッションが必要。

> ソース: https://github.com/anthropics/claude-code-action/pull/1338 (2026-05)

### 2.4 出力パラメータ

| 出力 | 説明 |
|------|------|
| `execution_file` | Claude Code出力ファイルパス |
| `branch_name` | 作成されたブランチ名 |
| `github_token` | 使用されたトークン |
| `structured_output` | JSONスキーマ結果 |
| `session_id` | 再開可能な会話ID |

### 2.5 ワークフロー例: 基本的なインタラクティブアシスタント

```yaml
name: Claude Assistant
on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]

jobs:
  claude-response:
    runs-on: ubuntu-latest
    steps:
      - uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
```

### 2.6 ワークフロー例: 自動PRコードレビュー

```yaml
name: Claude Auto Review
on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      id-token: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          prompt: |
            REPO: ${{ github.repository }}
            PR NUMBER: ${{ github.event.pull_request.number }}

            Please review this pull request with a focus on:
            - Code quality and best practices
            - Potential bugs or issues
            - Security implications
            - Performance considerations

            Note: The PR branch is already checked out in the current working directory.

            Use `gh pr comment` for top-level feedback.
            Use `mcp__github_inline_comment__create_inline_comment` (with `confirmed: true`) to highlight specific code issues.
            Only post GitHub comments - don't submit review text as messages.

          claude_args: |
            --allowedTools "mcp__github_inline_comment__create_inline_comment,Bash(gh pr comment:*),Bash(gh pr diff:*),Bash(gh pr view:*)"
```

### 2.7 ワークフロー例: 進捗トラッキング付きレビュー

```yaml
name: Claude Auto Review with Tracking
on:
  pull_request:
    types: [opened, synchronize, ready_for_review, reopened]

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      id-token: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          track_progress: true
          prompt: |
            REPO: ${{ github.repository }}
            PR NUMBER: ${{ github.event.pull_request.number }}

            Please review this pull request with a focus on:
            - Code quality and best practices
            - Potential bugs or issues
            - Security implications
            - Performance considerations

            Provide detailed feedback using inline comments for specific issues.

          claude_args: |
            --allowedTools "mcp__github_inline_comment__create_inline_comment,Bash(gh pr comment:*),Bash(gh pr diff:*),Bash(gh pr view:*)"
```

### 2.8 ワークフロー例: セキュリティレビュー（OWASP Top 10）

```yaml
name: Security Review
on:
  pull_request:
    types: [opened, synchronize]

jobs:
  security:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      security-events: write
      id-token: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          prompt: |
            REPO: ${{ github.repository }}
            PR NUMBER: ${{ github.event.pull_request.number }}

            Perform a comprehensive security review:

            ## OWASP Top 10 Analysis
            - SQL Injection vulnerabilities
            - Cross-Site Scripting (XSS)
            - Broken Authentication
            - Sensitive Data Exposure
            - XML External Entities (XXE)
            - Broken Access Control
            - Security Misconfiguration
            - Cross-Site Request Forgery (CSRF)
            - Using Components with Known Vulnerabilities
            - Insufficient Logging & Monitoring

            ## Additional Security Checks
            - Hardcoded secrets or credentials
            - Insecure cryptographic practices
            - Unsafe deserialization
            - Server-Side Request Forgery (SSRF)
            - Race conditions or TOCTOU issues

            Rate severity as: CRITICAL, HIGH, MEDIUM, LOW, or NONE.
            Post detailed findings with recommendations.

          claude_args: |
            --allowedTools "mcp__github_inline_comment__create_inline_comment,Bash(gh pr comment:*),Bash(gh pr diff:*)"
```

### 2.9 ワークフロー例: パス限定レビュー（認証/APIファイル）

```yaml
name: Review Critical Files
on:
  pull_request:
    types: [opened, synchronize]
    paths:
      - "src/auth/**"
      - "src/api/**"
      - "config/security.yml"

jobs:
  security-review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      id-token: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          prompt: |
            REPO: ${{ github.repository }}
            PR NUMBER: ${{ github.event.pull_request.number }}

            This PR modifies critical authentication or API files.

            Please provide a security-focused review with emphasis on:
            - Authentication and authorization flows
            - Input validation and sanitization
            - SQL injection or XSS vulnerabilities
            - API security best practices

            Post detailed security findings as PR comments.

          claude_args: |
            --allowedTools "mcp__github_inline_comment__create_inline_comment,Bash(gh pr comment:*)"
```

### 2.10 ワークフロー例: 外部コントリビューターレビュー

```yaml
name: External Contributor Review
on:
  pull_request:
    types: [opened, synchronize]

jobs:
  external-review:
    if: github.event.pull_request.author_association == 'FIRST_TIME_CONTRIBUTOR'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      id-token: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          prompt: |
            REPO: ${{ github.repository }}
            PR NUMBER: ${{ github.event.pull_request.number }}
            CONTRIBUTOR: ${{ github.event.pull_request.user.login }}

            This is a first-time contribution from @${{ github.event.pull_request.user.login }}.

            Please provide a comprehensive review focusing on:
            - Compliance with project coding standards
            - Proper test coverage (unit and integration)
            - Documentation for new features
            - Potential breaking changes
            - License header requirements

            Be welcoming but thorough in your review.

          claude_args: |
            --allowedTools "mcp__github_inline_comment__create_inline_comment,Bash(gh pr comment:*),Bash(gh pr view:*)"
```

### 2.11 ワークフロー例: PRレビューチェックリスト

```yaml
name: PR Review Checklist
on:
  pull_request:
    types: [opened, synchronize]

jobs:
  checklist-review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      id-token: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          prompt: |
            REPO: ${{ github.repository }}
            PR NUMBER: ${{ github.event.pull_request.number }}

            Review this PR against our team checklist:

            ## Code Quality
            - [ ] Code follows our style guide
            - [ ] No commented-out code
            - [ ] Meaningful variable names
            - [ ] DRY principle followed

            ## Testing
            - [ ] Unit tests for new functions
            - [ ] Integration tests for new endpoints
            - [ ] Edge cases covered
            - [ ] Test coverage > 80%

            ## Documentation
            - [ ] README updated if needed
            - [ ] API docs updated
            - [ ] Inline comments for complex logic
            - [ ] CHANGELOG.md updated

            ## Security
            - [ ] No hardcoded credentials
            - [ ] Input validation implemented
            - [ ] Proper error handling
            - [ ] No sensitive data in logs

            Post a summary comment with checklist results.

          claude_args: |
            --allowedTools "mcp__github_inline_comment__create_inline_comment,Bash(gh pr comment:*)"
```

### 2.12 ワークフロー例: 週次リポジトリメンテナンス

```yaml
name: Weekly Maintenance
on:
  schedule:
    - cron: "0 0 * * 0"
  workflow_dispatch:

jobs:
  maintenance:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
      id-token: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          prompt: |
            REPO: ${{ github.repository }}

            Perform weekly repository maintenance:

            1. Check for outdated dependencies in package.json
            2. Scan for security vulnerabilities using `npm audit`
            3. Review open issues older than 90 days
            4. Check for TODO comments in recent commits
            5. Verify README.md examples still work

            Create a single issue summarizing any findings.

          claude_args: |
            --allowedTools "Read,Bash(npm:*),Bash(gh issue:*),Bash(git:*)"
```

### 2.13 ワークフロー例: Issue自動トリアージ・ラベリング

```yaml
name: Issue Triage
on:
  issues:
    types: [opened]

jobs:
  triage:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          prompt: |
            REPO: ${{ github.repository }}
            ISSUE NUMBER: ${{ github.event.issue.number }}
            TITLE: ${{ github.event.issue.title }}
            BODY: ${{ github.event.issue.body }}

            Analyze this new issue and:
            1. Determine if it's a bug report, feature request, or question
            2. Assess priority (critical, high, medium, low)
            3. Suggest appropriate labels
            4. Check if it duplicates existing issues

          claude_args: |
            --allowedTools "Bash(./scripts/gh.sh:*),Bash(./scripts/edit-issue-labels.sh:*)"
```

### 2.14 ワークフロー例: APIドキュメント自動同期

```yaml
name: Sync API Documentation
on:
  pull_request:
    types: [opened, synchronize]
    paths:
      - "src/api/**/*.ts"
      - "src/routes/**/*.ts"

jobs:
  doc-sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      id-token: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0

      - uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          prompt: |
            REPO: ${{ github.repository }}
            PR NUMBER: ${{ github.event.pull_request.number }}

            This PR modifies API endpoints. Please:
            1. Review the API changes in src/api and src/routes
            2. Update API.md to document any new or changed endpoints
            3. Ensure OpenAPI spec is updated if needed
            4. Update example requests/responses

            Commit any documentation updates to this PR branch.

          claude_args: |
            --allowedTools "Read,Write,Edit,Bash(git:*)"
```

### 2.15 ワークフロー例: 構造化出力（Flaky Test検出）

```yaml
- name: Detect flaky tests
  id: analyze
  uses: anthropics/claude-code-action@v1
  with:
    anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
    prompt: Check if this is a flaky test failure
    claude_args: |
      --json-schema '{"type":"object","properties":{"is_flaky":{"type":"boolean"},"confidence":{"type":"number"}}}'

- name: Retry if flaky
  if: fromJSON(steps.analyze.outputs.structured_output).is_flaky == true
  run: gh workflow run CI
```

### 2.16 ワークフロー例: 高度な設定（CLIパラメータ指定）

```yaml
- uses: anthropics/claude-code-action@v1
  with:
    prompt: "Review this PR for performance issues"
    claude_args: |
      --max-turns 15
      --model claude-4-0-sonnet-20250805
      --allowedTools Edit,Read,Write,Bash
      --system-prompt "You are a performance optimization expert."
    anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
```

### 2.17 MCP サーバー設定

```yaml
# Sequential Thinkingサーバー
claude_args: |
  --mcp-config '{"mcpServers": {"sequential-thinking": {"command": "npx", "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"]}}}'
  --allowedTools mcp__sequential-thinking__sequentialthinking
```

```json
// シークレット付きMCPサーバー
{
  "mcpServers": {
    "custom-api-server": {
      "command": "npx",
      "args": ["-y", "@example/api-server"],
      "env": {
        "API_KEY": "${{ secrets.CUSTOM_API_KEY }}",
        "BASE_URL": "https://api.example.com"
      }
    }
  }
}
```

```json
// Python MCPサーバー（uv使用）
{
  "mcpServers": {
    "my-python-server": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "--directory",
        "${{ github.workspace }}/path/to/server/",
        "run",
        "server_file.py"
      ]
    }
  }
}
```

```yaml
# 複数MCPサーバーの同時指定
claude_args: |
  --mcp-config /tmp/config1.json
  --mcp-config /tmp/config2.json
  --mcp-config '{"mcpServers": {"inline-server": {"command": "npx", "args": ["@example/server"]}}}'
```

### 2.18 カスタム設定ファイル

```json
{
  "model": "claude-opus-4-1-20250805",
  "env": {
    "DEBUG": "true",
    "API_URL": "https://api.example.com"
  },
  "permissions": {
    "allow": ["Bash", "Read"],
    "deny": ["WebFetch"]
  },
  "hooks": {
    "PreToolUse": [{
      "matcher": "Bash",
      "hooks": [{
        "type": "command",
        "command": "echo Running bash command..."
      }]
    }]
  }
}
```

### 2.19 モード検出

| モード | トリガー | 動作 |
|--------|----------|------|
| Interactive | `@claude` メンション、issue割り当て、ラベル付与 | トラッキングコメント作成、応答返信 |
| Automation | `prompt` パラメータ指定時 | 即座実行、トラッキングなし（`track_progress` で有効化可能） |

### 2.20 対応GitHubイベント

`pull_request`, `pull_request_target`, `issue_comment`, `pull_request_comment`, `issues`, `pull_request_review`, `pull_request_review_comment`, `repository_dispatch`, `workflow_dispatch`

---

## 3. スケジュール実行

> ソース: https://github.com/shanraisshan/claude-code-best-practice

### 3.1 /loop（ローカル定期実行）

`/loop` コマンドでプロンプトやスラッシュコマンドをローカルで定期実行できる（最大3日間）。

```
/loop 5m /foo          # 5分間隔で /foo を実行
/loop 10m "check status"  # 10分間隔でステータス確認（デフォルト10分）
```

### 3.2 /schedule（クラウド定期実行 = Remote Trigger）

`/schedule` はAnthropicインフラ上でクラウド実行される。ローカルマシンがオフラインでも継続動作する。

```
/schedule              # スケジュール管理（作成・一覧・更新・削除）
```

cron式でスケジュール指定し、リモートエージェントとして実行される。

### 3.3 GitHub Actions cron

GitHub Actionsの `schedule` トリガーでcron実行可能:

```yaml
on:
  schedule:
    - cron: "0 0 * * 0"  # 毎週日曜0時
  workflow_dispatch:       # 手動実行も可能
```

### 3.4 Ralph Wiggum Loop Plugin

長時間タスク用の自律開発ループプラグイン。完了するまで繰り返し実行する。

---

## 4. Agent SDK

### 4.1 Python SDK

> ソース: https://github.com/anthropics/claude-agent-sdk-python

**インストール:**

```bash
pip install claude-agent-sdk
```

- Python 3.10+ 必須
- Claude Code CLIは自動バンドル（別途インストール不要）

**基本使用法 - query():**

```python
import anyio
from claude_agent_sdk import query

async def main():
    async for message in query(prompt="What is 2 + 2?"):
        print(message)

anyio.run(main)
```

**ClaudeSDKClient（双方向会話）:**

`ClaudeSDKClient` はカスタムツール・フックに対応した双方向インタラクティブセッションを提供。`query()` では利用できない機能を含む。

**カスタムツール（インプロセスMCPサーバー）:**

```python
from claude_agent_sdk import tool, create_sdk_mcp_server

@tool("greet", "Greet a user", {"name": str})
async def greet_user(args):
    return {
        "content": [{"type": "text", "text": f"Hello, {args['name']}!"}]
    }

server = create_sdk_mcp_server(
    name="my-tools",
    version="1.0.0",
    tools=[greet_user]
)
```

利点:
- サブプロセス管理が不要
- IPC オーバーヘッド削減
- 型安全な関数呼び出し

**フック（エージェント制御）:**

```python
async def check_bash_command(input_data, tool_use_id, context):
    if input_data["tool_name"] != "Bash":
        return {}
    # コマンドの安全性を評価...
    return {"hookSpecificOutput": {...}}
```

`PreToolUse` フックでツール使用前に介入し、許可・ブロックを制御可能。

**ツール権限管理:**

- `allowed_tools`: リストされたツールは自動承認
- `disallowed_tools`: 明示的にブロック
- 未リストのツールは `permission_mode` と `can_use_tool` で判定

**エラーハンドリング:**

| 例外クラス | 説明 |
|-----------|------|
| `ClaudeSDKError` | 基底クラス |
| `CLINotFoundError` | Claude Code未インストール |
| `CLIConnectionError` | 接続失敗 |
| `ProcessError` | プロセス終了（exit code付き） |
| `CLIJSONDecodeError` | レスポースパース失敗 |

**混合サーバーサポート:**

SDK MCPサーバー（インプロセス）と外部MCPサーバーを同時に登録・併用可能。

### 4.2 TypeScript SDK

> ソース: https://github.com/anthropics/claude-agent-sdk-demos （hello-world）

**インストール:**

```bash
npm install @anthropic-ai/claude-agent-sdk typescript @types/node tsx zod
```

**基本使用法:**

```typescript
import { query } from '@anthropic-ai/claude-agent-sdk';

const q = query({
  prompt: 'Your prompt here',
  options: {
    maxTurns: 100,        // 最大ターン数
    cwd: './workspace',   // 作業ディレクトリ
    model: 'sonnet',      // モデル指定
    executable: 'node',   // 実行バイナリ
    allowedTools: ['Read', 'Write', 'Edit', 'Bash', 'Glob', 'Grep']
  }
});

for await (const message of q) {
  if (message.type === 'assistant' && message.message) {
    const textContent = message.message.content.find(
      (c: any) => c.type === 'text'
    );
    if (textContent && 'text' in textContent) {
      console.log(textContent.text);
    }
  }
}
```

**フック実装:**

```typescript
hooks: {
  PreToolUse: [
    {
      matcher: "Write|Edit|MultiEdit",
      hooks: [
        async (input: any): Promise<HookJSONOutput> => {
          // return { continue: true } で許可
          // return { decision: 'block', stopReason: '...', continue: false } でブロック
        }
      ]
    }
  ]
}
```

**利用可能ツール:**

ファイル操作: `Read`, `Write`, `Edit`
検索: `Glob`, `Grep`, `WebSearch`
実行: `Bash`, `Task`
ユーティリティ: `TodoWrite`, `WebFetch`

### 4.3 デモプロジェクト 8種

> ソース: https://github.com/anthropics/claude-agent-sdk-demos

**注意:** これらはローカル開発用デモ。本番利用は想定外。

| # | デモ名 | 説明 |
|---|--------|------|
| 1 | **Hello World** | SDK基礎の入門サンプル |
| 2 | **Hello World V2** | V2 Session API（send/stream分離、マルチターン、セッション永続化） |
| 3 | **Email Agent** | IMAP対応メールアシスタント（受信管理、検索、AI支援） |
| 4 | **Excel Demo** | スプレッドシート/Excelファイル操作 |
| 5 | **Research Agent** | マルチエージェント並列リサーチ（後述） |
| 6 | **AskUserQuestion Previews** | HTML カードUIで選択肢表示、WebSocket + Plan Mode |
| 7 | **Simple Chat App** | React/Express WebSocketチャット |
| 8 | **Resume Generator** | Web検索→1ページ.docx履歴書自動生成 |

**前提条件:** Bun または Node.js 18+、有効なAnthropic APIキー

### 4.4 Research Agent（マルチエージェント並列の詳細）

> ソース: https://github.com/anthropics/claude-agent-sdk-demos/research-agent

**アーキテクチャ:**

1. **Lead Agent**: `Task` ツールでリサーチ全体を統括、2-4サブトピックに分解
2. **Researcher**: `WebSearch` + `Write` でウェブ情報収集（**並列スポーン**）
3. **Data Analyst**: `Glob`, `Read`, `Bash`, `Write` でメトリクス抽出・チャート生成
4. **Report Writer**: `Skill`, `Write`, `Glob`, `Read`, `Bash` でPDFレポート生成

**並列実行パターン:**

- Lead AgentがResearcherサブエージェントを**並列にスポーン**
- 各Researcherは独立して `files/research_notes/` に調査結果を保存
- Data Analystが結果を処理し `files/charts/` にチャート生成

**トラッキング:**

SDKフックが全ツール呼び出しをインターセプト。`parent_tool_use_id` でサブエージェントの起源を追跡。ログは人間可読トランスクリプトと構造化JSONL形式の両方で記録。

### 4.5 Managed Agents API 更新（2026-05）

> ソース: https://github.com/anthropics/skills/commit/d211d43
> ソース: https://github.com/anthropics/skills/commit/690f15c

v2.1.145〜v2.1.150 の期間に追加されたManaged Agents関連の新機能:

**Session Threads API（新規）:**
- `sessions.threads.list` / `retrieve` / `archive`
- `sessions.threads.events.list` / `stream`
- セッション内のスレッドを独立して追跡・アーカイブ可能

**Webhooks（新規）:**
- Webhookエンドポイント登録
- HMAC署名検証
- セッションイベントをHTTPで外部に配信

**Credentials API 更新:**
- `vaults.credentials.mcp_oauth_validate` 追加（MCP OAuth検証用）

**self-hosted sandboxes（新規）:**
- `config: {type: "self_hosted"}` でエージェントループをAnthropicのオーケストレーションに、ツール実行を自社インフラで行うハイブリッド構成
- アウトバウンドポーリングワーカー経由

**mid-session agent update（新規）:**
- `sessions.update(session_id, agent={tools, mcp_servers}, vault_ids=[...])` でセッション実行中にエージェント設定を変更可能
- エージェントバージョンは上げずにセッションローカルのオーバーライドとして機能

---

## 5. 並列実行

> ソース: https://github.com/shanraisshan/claude-code-best-practice
> ソース: https://github.com/affaan-m/everything-claude-code

### 5.1 Git Worktree

各エージェントに独立した作業コピーを与え、同一リポジトリで複数Claude Codeセッションを同時実行する。

```bash
# worktreeを作成
git worktree add ../feature-auth feature/auth
git worktree add ../feature-api feature/api

# 各worktreeで別々のClaude Codeを起動
cd ../feature-auth && claude
cd ../feature-api && claude
```

メリット:
- ブランチ間の干渉なし
- 各エージェントが独立したファイルシステムで動作
- 同一リポジトリのgitヒストリーを共有

### 5.2 Cascade Method

段階的にタスクを分解し、複数のClaude Codeインスタンスにカスケード（連鎖）させる手法。

1. **計画フェーズ**: 1つ目のインスタンスでタスク分解・計画策定
2. **実行フェーズ**: 計画に基づき複数インスタンスを並列起動
3. **統合フェーズ**: 結果をマージ

### 5.3 /fork コマンド

現在のコンテキストをフォークして新しいエージェントコンテキストを生成する。Skills の「progressive disclosure」と連携し、コンテキストを分岐させて並列作業を実現。

### 5.4 2インスタンスキックオフパターン

ターミナル（iTerm/Ghostty/tmux推奨、IDE非推奨）で複数Claude Codeセッションを同時起動:

```bash
# ターミナル1
claude   # /rename backend

# ターミナル2
claude   # /rename frontend
```

- `/rename` でインスタンスにラベル付け
- `/resume` で中断セッションの再開
- IDEではなくターミナルベースを推奨（パフォーマンス上の理由）

### 5.5 Agent Teams

複数エージェントが同一コードベースで並列作業し、共有タスク調整を行う。ビルトイン環境変数により隔離コンテキスト間での並行タスク実行が可能。

### 5.6 サブエージェント委譲

```
"use subagents" と指示してより多くのコンピュートを問題に投入
```

タスクをサブエージェントにオフロードし、メインコンテキストを集中させる。テスト時コンピュートを別コンテキストウィンドウで強化。

---

## 6. オーケストレーター

> ソース: https://github.com/affaan-m/everything-claude-code
> ソース: https://github.com/shanraisshan/claude-code-best-practice

### 6.1 Claude Squad

複数のClaude Codeインスタンスを統括管理するオーケストレーションツール。チーム単位でエージェントを管理し、タスク分配・進捗監視を行う。

### 6.2 Ruflo

タスクベースのエージェントオーケストレーター。プロジェクト管理とエージェント実行を統合。

### 6.3 Vibe Kanban

カンバンボード形式でエージェントタスクを視覚的に管理。タスクの進捗をボード上で追跡。

### 6.4 agent-orchestrator

汎用的なエージェントオーケストレーションフレームワーク。

### 6.5 TSK

タスク管理に特化したオーケストレーションツール。

### 6.6 /orchestrate コマンド

> ソース: https://github.com/affaan-m/everything-claude-code

マルチエージェントワークフローを調整するビルトインコマンド。

関連コマンド群（`ccg-workflow` ランタイム必要）:
- `/multi-plan` - マルチエージェント計画
- `/multi-execute` - マルチエージェント実行
- `/multi-backend` - バックエンド並列
- `/multi-frontend` - フロントエンド並列
- `/multi-workflow` - オーケストレーテッドワークフロー

```bash
npx ccg-workflow  # ランタイム初期化
```

### 6.7 PM2 サービス管理

`/pm2` コマンドでPM2ベースのサービスライフサイクル管理。複雑なマルチサービスワークフロー向け。6つの関連コマンドを提供。

### 6.8 Command → Agent → Skill パターン

3層オーケストレーション階層:

| 層 | 役割 | 特徴 |
|----|------|------|
| **Command** | 既存コンテキストにナレッジ注入 | 軽量 |
| **Agent** | 新しい隔離コンテキストで動作 | 独立実行 |
| **Skill** | Progressive Disclosureでコンテキストをフォーク | 段階的開示 |

---

## 7. マルチモデル並列・トークン予算管理

> ソース: https://github.com/shanraisshan/claude-code-best-practice
> ソース: https://github.com/affaan-m/everything-claude-code

### 7.1 クロスモデルワークフロー

Claude CodeとCodex CLIを組み合わせ、計画/実装レビューを同時に行うマルチモデル並列分析。

### 7.2 モデル選択戦略

```json
{
  "model": "sonnet",
  "env": {
    "MAX_THINKING_TOKENS": "10000",
    "CLAUDE_AUTOCOMPACT_PCT_OVERRIDE": "50",
    "CLAUDE_CODE_SUBAGENT_MODEL": "haiku"
  }
}
```

| 用途 | モデル | コスト削減 |
|------|--------|-----------|
| ルーティン作業 | `/model sonnet` | 約60%削減 |
| 複雑なアーキテクチャ/デバッグ/深い推論 | `/model opus` | - |
| サブエージェント | `haiku` | 最大コスト効率 |

### 7.3 トークン予算管理

**コンテキスト監視:**
- `/context` でトークン使用量を確認
- `/compact` は最大50%時点で実行（「agent dumb zone」回避）
- compactionエラー時は1Mトークンモデルに切替後に再compact

**セッション管理:**
- `/clear` でタスク切替時にコンテキストリセット
- `/extra-usage` でオーバーフロー課金設定（拡張トークン予算）
- チェックポイント機能（`Esc Esc` or `/rewind`）でファイル編集追跡（gitベースではなくClaudeのファイル操作追跡による）

**MCP制約:**
- プロジェクトあたり **10 MCP以下** を推奨
- アクティブツール **80以下** を推奨
- 超過時: コンテキストウィンドウが200k → 約70kに縮小

### 7.4 AgentShield（セキュリティスキャン in CI/CD）

```bash
npx ecc-agentshield scan --fix           # 安全な問題を自動修正
npx ecc-agentshield scan --opus --stream  # 深い分析
```

CI/CDビルドゲートとして、クリティカル検出時にexit code 2を返す。

---

## 付録: ソースURL一覧

| # | ソース | URL |
|---|--------|-----|
| 1 | Anthropic Docs (Advanced) | https://docs.anthropic.com/en/docs/claude-code/advanced |
| 2 | Anthropic Docs (GitHub Actions) | https://docs.anthropic.com/en/docs/claude-code/github-actions |
| 3 | claude-code-action README | https://github.com/anthropics/claude-code-action |
| 4 | claude-code-action Solutions | https://github.com/anthropics/claude-code-action/blob/main/docs/solutions.md |
| 5 | claude-code-action Usage | https://github.com/anthropics/claude-code-action/blob/main/docs/usage.md |
| 6 | claude-code-action Config | https://github.com/anthropics/claude-code-action/blob/main/docs/configuration.md |
| 7 | claude-code-action Custom Automations | https://github.com/anthropics/claude-code-action/blob/main/docs/custom-automations.md |
| 8 | claude-agent-sdk-python | https://github.com/anthropics/claude-agent-sdk-python |
| 9 | claude-agent-sdk-demos | https://github.com/anthropics/claude-agent-sdk-demos |
| 10 | claude-code-best-practice | https://github.com/shanraisshan/claude-code-best-practice |
| 11 | everything-claude-code | https://github.com/affaan-m/everything-claude-code |
| 12 | claude-code-action WIF PR | https://github.com/anthropics/claude-code-action/pull/1338 |
| 13 | claude-code-action WIF example | https://github.com/anthropics/claude-code-action/blob/main/examples/claude-wif.yml |
| 14 | Managed Agents Webhooks | https://platform.claude.com/docs/en/managed-agents/webhooks.md |
| 15 | Managed Agents Multi-Agent | https://platform.claude.com/docs/en/managed-agents/multi-agent.md |
