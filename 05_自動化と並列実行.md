# 05. 自動化と並列実行 -- automation-evaluator ナレッジベース

> 本ドキュメントは `research/05_自動化と並列実行.md` を元に構造化したルール集である。
> automation-evaluator エージェントが評価判定に使用する。

---

## 1. Headlessモード

### R-001: `-p` / `--print` フラグで非対話モードを起動する

```bash
claude -p "このプロジェクトの構造を説明して"
```

### R-002: `--output-format` で出力形式を指定する

`text`（デフォルト）、`json`、`streaming-json` の3種がある。

```bash
claude -p "READMEを要約して" --output-format json
claude -p "テスト追加して" --output-format streaming-json
```

### R-003: `--session-id` でセッションを指定して会話を継続する

```bash
claude -p "前の続き" --session-id my-session-123
```

### R-004: `--resume` は直前のセッションを再開、`--continue` は直前のコンテキストを引き継いで新規プロンプトを実行する

### R-005: `--max-turns` でエージェントループの最大ターン数を制限する

```bash
claude -p "バグを修正して" --max-turns 10
```

### R-006: `--allowedTools` / `--disallowedTools` で使用ツールを制御する

```bash
claude -p "コードレビューして" --allowedTools "Read,Grep,Glob"
```

### R-007: CI/CD環境では `ANTHROPIC_API_KEY` を環境変数に設定し、`--allowedTools` でツールを制限する

```bash
export ANTHROPIC_API_KEY="sk-ant-..."
claude -p "npm testを実行して結果を報告" --allowedTools "Bash(npm:*),Read"
```

### R-008: V2 Session API は `send()` + `stream()` を分離し、マルチターン会話を実現する

V1の `query()` が全メッセージのAsync Generatorだったのに対し、V2はセッション概念を持つ。

```typescript
const session = unstable_v2_createSession({ model: 'sonnet' });
await session.send();
for await (const event of session.stream()) { /* イベント処理 */ }
```

### R-009: V2 Session API は `sessionId` でセッションを永続化・再開できる

```typescript
const resumed = unstable_v2_resumeSession(sessionId, { model: 'sonnet' });
```

### R-010: V2のワンショット実行には `unstable_v2_prompt()` を使う

```typescript
const result = unstable_v2_prompt('質問内容', { model: 'sonnet' });
```

### R-011: V1 vs V2 の主な違い

| 項目 | V1 (`query()`) | V2 (Session API) |
|------|----------------|-------------------|
| パターン | Async Generator | send() + stream() |
| 会話 | 単発プロンプト | マルチターン対応 |
| 永続化 | なし | sessionIdで再開可能 |
| ワンショット | query()で実行 | unstable_v2_prompt() |

**情報源:** https://docs.anthropic.com/en/docs/claude-code/advanced, https://github.com/anthropics/claude-agent-sdk-demos

---

## 2. GitHub Actions

### R-012: `anthropics/claude-code-action` はPR/Issueに対してClaude Codeを統合するGitHub Actionである

Stars 6.7k / License MIT / Latest v1.0（内部バージョン 2.1.195、2026-06-26更新）。認証は Anthropic API直接 / AWS Bedrock / Google Vertex AI / Microsoft Foundry に対応。GCP Workload Identity Federation（OIDC）もサポート。

### R-013: `claude /install-github-app` でGitHub App・シークレット・リポジトリセットアップを自動設定する

```bash
claude /install-github-app
```

### R-014: 主要入力パラメータ一覧

| パラメータ | デフォルト | 説明 |
|-----------|-----------|------|
| `anthropic_api_key` | - | Anthropic APIキー |
| `claude_code_oauth_token` | - | OAuth認証トークン |
| `prompt` | - | カスタムプロンプト指示 |
| `trigger_phrase` | `@claude` | コメントトリガーフレーズ |
| `assignee_trigger` | - | ユーザー名トリガー |
| `label_trigger` | `claude` | ラベルトリガー |
| `track_progress` | - | 進捗トラッキングコメント有効化 |
| `claude_args` | - | Claude CLIへの追加引数 |
| `include_fix_links` | `true` | PRレビューの「Fix this」リンク |
| `use_sticky_comment` | `false` | 単一コメントに統合 |
| `classify_inline_comments` | `true` | インラインコメント分類 |
| `branch_prefix` | `claude/` | ブランチ名プレフィックス |
| `use_commit_signing` | - | コミット署名有効化 |
| `settings` | - | JSON設定文字列またはファイルパス |
| `additional_permissions` | - | 追加権限 |
| `allowed_bots` | - | 許可するbotリスト |
| `plugins` | - | プラグインリスト |
| `use_bedrock` | - | AWS Bedrock OIDC有効化 |
| `use_vertex` | - | Google Vertex AI OIDC有効化 |

### R-015: 出力パラメータ一覧

| 出力 | 説明 |
|------|------|
| `execution_file` | Claude Code出力ファイルパス |
| `branch_name` | 作成されたブランチ名 |
| `github_token` | 使用されたトークン |
| `structured_output` | JSONスキーマ結果 |
| `session_id` | 再開可能な会話ID |

### R-016: 基本的なインタラクティブアシスタントは `issue_comment` と `pull_request_review_comment` イベントで起動する

```yaml
name: Claude Assistant
on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]

jobs:
  claude-response:
    runs-on: ubuntu-latest
    steps:
      - uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
```

### R-017: 自動PRコードレビューは `pull_request` の `opened` / `synchronize` イベントで起動する

必要な permissions: `contents: read`, `pull-requests: write`, `id-token: write`

```yaml
name: Claude Auto Review
on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      id-token: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1
      - uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          prompt: |
            REPO: ${{ github.repository }}
            PR NUMBER: ${{ github.event.pull_request.number }}
            Please review this pull request...
          claude_args: |
            --allowedTools "mcp__github_inline_comment__create_inline_comment,Bash(gh pr comment:*),Bash(gh pr diff:*),Bash(gh pr view:*)"
```

### R-018: `track_progress: true` で進捗トラッキングコメントを有効化する

### R-019: セキュリティレビューでは `security-events: write` 権限を追加し、OWASP Top 10 に基づく分析を指示する

### R-020: `paths` フィルタでレビュー対象を特定ディレクトリに限定できる

```yaml
on:
  pull_request:
    types: [opened, synchronize]
    paths:
      - "src/auth/**"
      - "src/api/**"
      - "config/security.yml"
```

### R-021: `github.event.pull_request.author_association` で外部コントリビューター向けレビューを分岐する

```yaml
if: github.event.pull_request.author_association == 'FIRST_TIME_CONTRIBUTOR'
```

### R-022: PRレビューチェックリストをプロンプトに埋め込み、構造化評価を実施できる

Code Quality / Testing / Documentation / Security の4カテゴリでチェックリストを構成する。

### R-023: 週次メンテナンスは `schedule` + `workflow_dispatch` トリガーで実行する

```yaml
on:
  schedule:
    - cron: "0 0 * * 0"
  workflow_dispatch:
```

`fetch-depth: 0` で全履歴を取得し、依存関係・脆弱性・古いIssue・TODOコメント・READMEの検証を行う。

### R-024: Issue自動トリアージは `issues: [opened]` イベントで起動し、分類・優先度・ラベル提案・重複検出を行う

### R-025: APIドキュメント自動同期はAPIファイル変更時にドキュメントを自動更新しPRブランチにコミットする

```yaml
paths:
  - "src/api/**/*.ts"
  - "src/routes/**/*.ts"
```

### R-026: `--json-schema` で構造化出力を取得し、後続ステップで条件分岐できる

```yaml
- name: Detect flaky tests
  id: analyze
  uses: anthropics/claude-code-action@v1
  with:
    prompt: Check if this is a flaky test failure
    claude_args: |
      --json-schema '{"type":"object","properties":{"is_flaky":{"type":"boolean"},"confidence":{"type":"number"}}}'

- name: Retry if flaky
  if: fromJSON(steps.analyze.outputs.structured_output).is_flaky == true
  run: gh workflow run CI
```

### R-027: `claude_args` でCLIパラメータを直接指定できる

```yaml
claude_args: |
  --max-turns 15
  --model claude-4-0-sonnet-20250805
  --allowedTools Edit,Read,Write,Bash
  --system-prompt "You are a performance optimization expert."
```

### R-028: `--mcp-config` でMCPサーバーをGitHub Actions内で使用する

インライン JSON、ファイルパス、複数指定のいずれも可能。

```yaml
claude_args: |
  --mcp-config '{"mcpServers": {"sequential-thinking": {"command": "npx", "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"]}}}'  
  --allowedTools mcp__sequential-thinking__sequentialthinking
```

### R-029: MCP サーバーはシークレットを `env` フィールドで注入できる

```json
{
  "mcpServers": {
    "custom-api-server": {
      "command": "npx",
      "args": ["-y", "@example/api-server"],
      "env": {
        "API_KEY": "${{ secrets.CUSTOM_API_KEY }}",
        "BASE_URL": "https://api.example.com"
      }
    }
  }
}
```

### R-030: Python MCPサーバーは `uv` コマンドで起動する

```json
{
  "mcpServers": {
    "my-python-server": {
      "type": "stdio",
      "command": "uv",
      "args": ["--directory", "${{ github.workspace }}/path/to/server/", "run", "server_file.py"]
    }
  }
}
```

### R-031: 複数MCPサーバーは `--mcp-config` を複数回指定して同時利用する

```yaml
claude_args: |
  --mcp-config /tmp/config1.json
  --mcp-config /tmp/config2.json
  --mcp-config '{"mcpServers": {"inline-server": {"command": "npx", "args": ["@example/server"]}}}'
```

### R-032: カスタム設定ファイルで model / env / permissions / hooks を一括指定する

```json
{
  "model": "claude-opus-4-1-20250805",
  "env": { "DEBUG": "true", "API_URL": "https://api.example.com" },
  "permissions": { "allow": ["Bash", "Read"], "deny": ["WebFetch"] },
  "hooks": {
    "PreToolUse": [{
      "matcher": "Bash",
      "hooks": [{ "type": "command", "command": "echo Running bash command..." }]
    }]
  }
}
```

### R-033: モード検出は自動で行われる

| モード | トリガー | 動作 |
|--------|----------|------|
| Interactive | `@claude` メンション、issue割り当て、ラベル付与 | トラッキングコメント作成、応答返信 |
| Automation | `prompt` パラメータ指定時 | 即座実行、トラッキングなし（`track_progress` で有効化可能） |

### R-034: 対応サイトGitHubイベント一覧

`pull_request`, `pull_request_target`, `issue_comment`, `pull_request_comment`, `issues`, `pull_request_review`, `pull_request_review_comment`, `repository_dispatch`, `workflow_dispatch`

**情報源:** https://docs.anthropic.com/en/docs/claude-code/github-actions, https://github.com/anthropics/claude-code-action, https://github.com/anthropics/claude-code-action/blob/main/docs/solutions.md, https://github.com/anthropics/claude-code-action/blob/main/docs/usage.md, https://github.com/anthropics/claude-code-action/blob/main/docs/configuration.md, https://github.com/anthropics/claude-code-action/blob/main/docs/custom-automations.md

---

## 3. スケジュール実行

### R-035: `/loop` でローカル定期実行する（最大３日間）

```
/loop 5m /foo          # 5分間隔で /foo を実行
/loop 10m "check status"  # 10分間隔でステータス確認（デフォルト10分）
```

### R-036: `/schedule` はAnthropicインフラ上のクラウド定期実行（Remote Trigger）である

ローカルマシンがオフラインでも継続動作する。cron式でスケジュール指定。

```
/schedule   # スケジュール管理（作成・一覧・更新・削除）
```

### R-037: GitHub Actions cron で定期実行する場合は `schedule` + `workflow_dispatch` を併用する

```yaml
on:
  schedule:
    - cron: "0 0 * * 0"  # 毎週日曜0時
  workflow_dispatch:       # 手動実行も可能
```

### R-038: Ralph Wiggum Loop Plugin は長時間タスク用の自律開発ループプラグインである

完了するまで繰り返し実行する。

**情報源:** https://github.com/shanraisshan/claude-code-best-practice

---

## 4. Agent SDK

### 4.1 Python SDK

### R-039: Python SDKは `pip install claude-agent-sdk` でインストールする（Python 3.10+必須）

Claude Code CLIは自動バンドルされるため別途インストール不要。最新バージョン: v0.2.110（2026-06-26）。

### R-040: `query()` でAsync Generatorベースの基本実行を行う

```python
import anyio
from claude_agent_sdk import query

async def main():
    async for message in query(prompt="What is 2 + 2?"):
        print(message)

anyio.run(main)
```

### R-041: `ClaudeSDKClient` はカスタムツール・フックに対応した双方向インタラクティブセッションを提供する

`query()` では利用できない機能を含む。

### R-042: `@tool` デコレータと `create_sdk_mcp_server` でインプロセスMCPサーバーを作成する

```python
from claude_agent_sdk import tool, create_sdk_mcp_server

@tool("greet", "Greet a user", {"name": str})
async def greet_user(args):
    return {"content": [{"type": "text", "text": f"Hello, {args['name']}!"}]}

server = create_sdk_mcp_server(name="my-tools", version="1.0.0", tools=[greet_user])
```

利点: サブプロセス管理不要、IPCオーバーヘッド削減、型安全な関数呼び出し。

### R-043: `PreToolUse` フックでツール使用前に介入し許可・ブロックを制御する

```python
async def check_bash_command(input_data, tool_use_id, context):
    if input_data["tool_name"] != "Bash":
        return {}
    # コマンドの安全性を評価...
    return {"hookSpecificOutput": {...}}
```

### R-044: ツール権限は `allowed_tools`（自動承認）、`disallowed_tools`（明示ブロック）、`permission_mode` + `can_use_tool` の3層で管理する

### R-045: Python SDKのエラーハンドリング例外クラス

| 例外クラス | 説明 |
|-----------|------|
| `ClaudeSDKError` | 基底クラス |
| `CLINotFoundError` | Claude Code未インストール |
| `CLIConnectionError` | 接続失敗 |
| `ProcessError` | プロセス終了（exit code付き） |
| `CLIJSONDecodeError` | レスポンスパース失敗 |

### R-046: SDK MCPサーバー（インプロセス）と外部MCPサーバーを同時に登録・併用できる

### R-046a: `SessionStore` プロトコルでセッションをPostgreSQL・Redis・S3に永続化できる（v0.2.x新機能）

`SessionStore` はプロトコル（インターフェース）として定義されており、参照実装がサンプルとして提供されている。

```python
from claude_agent_sdk.testing import run_session_store_conformance
import pytest

@pytest.mark.anyio
async def test_my_store_conformance():
    await run_session_store_conformance(lambda: MyStore(...))
```

| バックエンド | モジュール | 依存ライブラリ |
|------------|-----------|---------------|
| PostgreSQL | `examples/session_stores/postgres_session_store.py` | asyncpg |
| Redis | `examples/session_stores/redis_session_store.py` | redis-py (fakeredis for test) |
| S3 | `examples/session_stores/s3_session_store.py` | boto3 (moto for test) |

`append()` 失敗は `MirrorErrorMessage` としてログされ、会話をブロックしない。

### R-046b: `TaskUpdatedMessage` でバックグラウンドタスクの終了を確実に検出できる（v0.2.101+）

`task_updated` ライフサイクルイベントが `TaskUpdatedMessage` として型付きで公開された。フィールド: `task_id`, `patch`, `status`, `session_id`, `uuid`。`TERMINAL_TASK_STATUSES` frozensetで終端状態を判定できる。

**情報源:** https://github.com/anthropics/claude-agent-sdk-python（v0.2.101 CHANGELOG, v0.2.110まで更新）

### 4.2 TypeScript SDK

### R-047: TypeScript SDKは `npm install @anthropic-ai/claude-agent-sdk typescript @types/node tsx zod` でインストールする

### R-048: TypeScript SDK の `query()` でストリーミング実行する

```typescript
import { query } from '@anthropic-ai/claude-agent-sdk';

const q = query({
  prompt: 'Your prompt here',
  options: {
    maxTurns: 100,
    cwd: './workspace',
    model: 'sonnet',
    executable: 'node',
    allowedTools: ['Read', 'Write', 'Edit', 'Bash', 'Glob', 'Grep']
  }
});

for await (const message of q) {
  if (message.type === 'assistant' && message.message) {
    const textContent = message.message.content.find((c: any) => c.type === 'text');
    if (textContent && 'text' in textContent) {
      console.log(textContent.text);
    }
  }
}
```

### R-049: TypeScript SDKでもフック（`PreToolUse`）でツール使用を制御できる

```typescript
hooks: {
  PreToolUse: [
    {
      matcher: "Write|Edit|MultiEdit",
      hooks: [
        async (input: any): Promise<HookJSONOutput> => {
          // return { continue: true } で許可
          // return { decision: 'block', stopReason: '...', continue: false } でブロック
        }
      ]
    }
  ]
}
```

### R-050: SDK利用可能ツール一覧

| カテゴリ | ツール |
|----------|--------|
| ファイル操作 | `Read`, `Write`, `Edit` |
| 検索 | `Glob`, `Grep`, `WebSearch` |
| 実行 | `Bash`, `Task` |
| ユーティリティ | `TodoWrite`, `WebFetch` |

### 4.3 デモプロジェクト

### R-051: 公式デモプロジェクトは8種ありローカル開発用（本番利用は想定外）

| # | デモ名 | 説明 |
|---|--------|------|
| 1 | Hello World | SDK基礎の入門サンプル |
| 2 | Hello World V2 | V2 Session API（send/stream分離、マルチターン、セッション永続化） |
| 3 | Email Agent | IMAP対応メールアシスタント |
| 4 | Excel Demo | スプレッドシート/Excelファイル操作 |
| 5 | Research Agent | マルチエージェント並列リサーチ |
| 6 | AskUserQuestion Previews | HTML カードUIで選択肢表示、WebSocket + Plan Mode |
| 7 | Simple Chat App | React/Express WebSocketチャット |
| 8 | Resume Generator | Web検索から1ページ.docx履歴書自動生成 |

前提条件: Bun または Node.js 18+、有効なAnthropic APIキー。

### R-052: Research Agent はマルチエージェント並列リサーチのリファレンス実装である

**アーキテクチャ:**
1. **Lead Agent**: `Task` ツールでリサーチ全体を統括、2-4サブトピックに分解
2. **Researcher**: `WebSearch` + `Write` でウェブ情報収集（並列スポーン）
3. **Data Analyst**: `Glob`, `Read`, `Bash`, `Write` でメトリクス抽出・チャート生成
4. **Report Writer**: `Skill`, `Write`, `Glob`, `Read`, `Bash` でPDFレポート生成

### R-053: Research Agent では Lead Agent が Researcher サブエージェントを並列にスポーンする

各Researcherは独立して `files/research_notes/` に調査結果を保存。Data Analystが結果を処理し `files/charts/` にチャート生成。

### R-054: SDKフックが全ツール呼び出しをインターセプトし `parent_tool_use_id` でサブエージェントの起源を追跡する

ログは人間可読トランスクリプトと構造化JSONL形式の両方で記録。

**情報源:** https://github.com/anthropics/claude-agent-sdk-python, https://github.com/anthropics/claude-agent-sdk-demos

---

## 5. 並列実行

### R-055: Git Worktreeで各エージェントに独立した作業コピーを与える

```bash
git worktree add ../feature-auth feature/auth
git worktree add ../feature-api feature/api

# 各worktreeで別々のClaude Codeを起動
cd ../feature-auth && claude
cd ../feature-api && claude
```

メリット: ブランチ間の干渉なし、独立したファイルシステム、同一リポジトリのgitヒストリー共有。

### R-056: Cascade Methodは計画・実行・統合の3フェーズでタスクを分解し並列実行する

1. **計画フェーズ**: 1つ目のインスタンスでタスク分解・計画策定
2. **実行フェーズ**: 計画に基づき複数インスタンスを並列起動
3. **統合フェーズ**: 結果をマージ

### R-057: `/fork` で現在のコンテキストをフォークして新しいエージェントコンテキストを生成する

Skills の「progressive disclosure」と連携し、コンテキストを分岐させて並列作業を実現。

### R-058: 2インスタンスキックオフパターンではターミナルで複数セッションを同時起動する

```bash
# ターミナル1
claude   # /rename backend

# ターミナル2
claude   # /rename frontend
```

- `/rename` でインスタンスにラベル付け
- `/resume` で中断セッションの再開
- IDEではなくターミナルベースを推奨（パフォーマンス上の理由）

### R-059: Agent Teams で複数エージェントが同一コードベースで並列作業する

ビルトイン環境変数により隔離コンテキスト間での並行タスク実行が可能。

### R-060: 「use subagents」と指示してサブエージェントにタスクを委譲する

タスクをサブエージェントにオフロードし、メインコンテキストを集中させる。テスト時コンピュートを別コンテキストウィンドウで強化。

**情報源:** https://github.com/shanraisshan/claude-code-best-practice, https://github.com/affaan-m/everything-claude-code

---

## 6. オーケストレーター

### R-061: Claude Squad は複数のClaude Codeインスタンスを統括管理するオーケストレーションツールである

チーム単位でエージェントを管理し、タスク分配・進捗監視を行う。

### R-062: Ruflo はタスクベースのエージェントオーケストレーターである

プロジェクト管理とエージェント実行を統合。

### R-063: Vibe Kanban はカンバンボード形式でエージェントタスクを視覚的に管理する

### R-064: agent-orchestrator は汎用的なエージェントオーケストレーションフレームワークである

### R-065: TSK はタスク管理に特化したオーケストレーションツールである

### R-066: `/orchestrate` はマルチエージェントワークフローを調整するビルトインコマンドである

関連コマンド群（`ccg-workflow` ランタイム必要）:
- `/multi-plan` - マルチエージェント計画
- `/multi-execute` - マルチエージェント実行
- `/multi-backend` - バックエンド並列
- `/multi-frontend` - フロントエンド並列
- `/multi-workflow` - オーケストレーテッドワークフロー

```bash
npx ccg-workflow  # ランタイム初期化
```

### R-067: `/pm2` でPM2ベースのサービスライフサイクル管理を行う

複雑なマルチサービスワークフロー向けだ66つの関連コマンドを提供。

### R-068: 3層オーケストレーション階層 -- Command / Agent / Skill

| 層 | 役割 | 特徴 |
|----|------|------|
| **Command** | 既存コンテキストにナレッジ注入 | 軽量 |
| **Agent** | 新しい隔離コンテキストで動作 | 独立実行 |
| **Skill** | Progressive Disclosureでコンテキストをフォーク | 段階的開示 |

**情報源:** https://github.com/affaan-m/everything-claude-code, https://github.com/shanraisshan/claude-code-best-practice

---

## 7. マルチモデル並列・トークン予算管理

### R-069: クロスモデルワークフローでClaude CodeとCodex CLIを組み合わせて計画/実装レビューを同時に行う

### R-070: モデル選択は用途に応じて切り替える

```json
{
  "model": "sonnet",
  "env": {
    "MAX_THINKING_TOKENS": "10000",
    "CLAUDE_AUTOCOMPACT_PCT_OVERRIDE": "50",
    "CLAUDE_CODE_SUBAGENT_MODEL": "haiku"
  }
}
```

| 用途 | モデル | コスト削減 |
|------|--------|----------|
| ルーティン作業 | `/model sonnet` | 絉60%削減 |
| 複雑なアーキテクチャ/デバッグ/深い推論 | `/model opus` | - |
| サブエージェント | `haiku` | 最大コスト効率 |

### R-071: `/context` でトークン使用量を確認する

### R-072: `/compact` は最大50%時点で実行する（「agent dumb zone」回避）

compactionエラー時は1Mトークンモデルに切替後に再 compact。

### R-073: `/clear` でタスク切替時にコンテキストリセットする

### R-074: `/extra-usage` でオーバーフロー課金設定（拡張トークン予算）を行う

### R-075: チェックポイント機能（`Esc Esc` or `/rewind`）でgitベースの編集追跡を行う

### R-076: MCPサーバーはプロジェクトあたり10以下、アクティブツールは80以下を推奨する

超過時: コンテキストウィンドウが200k から絉70kに縮小。

### R-077: AgentShield はCI/CDビルドゲートとしてセキュリティスキャンを行う

```bash
npx ecc-agentshield scan --fix           # 安全な問題を自動修正
npx ecc-agentshield scan --opus --stream  # 深い分析
```

クリティカル検出時にexit code 2を返す。

**情報源:** https://github.com/shanraisshan/claude-code-best-practice, https://github.com/affaan-m/everything-claude-code

---

## 付録: ソースURL一覧

| # | ソース | URL |
|---|--------|-----|
| 1 | Anthropic Docs (Advanced) | https://docs.anthropic.com/en/docs/claude-code/advanced |
| 2 | Anthropic Docs (GitHub Actions) | https://docs.anthropic.com/en/docs/claude-code/github-actions |
| 3 | claude-code-action README | https://github.com/anthropics/claude-code-action |
| 4 | claude-code-action Solutions | https://github.com/anthropics/claude-code-action/blob/main/docs/solutions.md |
| 5 | claude-code-action Usage | https://github.com/anthropics/claude-code-action/blob/main/docs/usage.md |
| 6 | claude-code-action Config | https://github.com/anthropics/claude-code-action/blob/main/docs/configuration.md |
| 7 | claude-code-action Custom Automations | https://github.com/anthropics/claude-code-action/blob/main/docs/custom-automations.md |
| 8 | claude-agent-sdk-python | https://github.com/anthropics/claude-agent-sdk-python |
| 9 | claude-agent-sdk-demos | https://github.com/anthropics/claude-agent-sdk-demos |
| 10 | claude-code-best-practice | https://github.com/shanraisshan/claude-code-best-practice |
| 11 | everything-claude-code | https://github.com/affaan-m/everything-claude-code |

---

## automation-evaluator用 評価チェックリスト

以下のチェックリストは、Claude Codeの自動化・並列実行設定を評価する際に使用する。

### A. Headlessモード (R-001 -- R-011)

- [ ] `-p` フラグで非対話モードを正しく使用しているか
- [ ] `--output-format` が用途に適切か（CI/CDでは `json` または `streaming-json` 推奨）
- [ ] `--max-turns` でエージェントループに上限を設けているか
- [ ] `--allowedTools` / `--disallowedTools` で最小権限を適用しているか
- [ ] CI/CD環境で `ANTHROPIC_API_KEY` が安全に管理されているか
- [ ] V2 Session APIを使用する場合、マルチターン・永続化の要否を検討したか

### B. GitHub Actions (R-012 -- R-034)

- [ ] `anthropics/claude-code-action@v1` を使用しているか
- [ ] `permissions` が必要最小限に設定されているか
- [ ] `trigger_phrase` / `label_trigger` が適切に設定されているか
- [ ] PRレビューワークフローで `fetch-depth: 1` を指定しているか（週次メンテナンスは `fetch-depth: 0`）
- [ ] `--allowedTools` でツールを用途に応じて制限しているか
- [ ] セキュリティレビューでOWASP Top 10を網羅しているか
- [ ] 構造化出力（`--json-schema`）を活用して後続ステップと連携しているか
- [ ] MCPサーバー設定でシークレットを `env` 経由で注入しているか（ハードコード禁止）
- [ ] モード検出（Interactive / Automation）を理解して設計しているか

### C. スケジュール実行 (R-035 -- R-038)

- [ ] `/loop`（ローカル、最大3日）と `/schedule`（クラウド、オフライン対応）を用途に応じて使い分けているか
- [ ] GitHub Actions cronに `workflow_dispatch` を併用して手動実行も可能にしているか

### D. Agent SDK (R-039 -- R-054)

- [ ] Python SDK / TypeScript SDK のインストール要件を満たしているか
- [ ] `query()` と `ClaudeSDKClient` の使い分けが適切か
- [ ] カスタムツールをインプロセスMCPサーバーとして実装しているか
- [ ] `PreToolUse` フックでツール使用前の制御を実装しているか
- [ ] ツール権限（`allowed_tools` / `disallowed_tools`）を適切に設定しているか
- [ ] エラーハンドリングで適切な例外クラスをキャッチしているか
- [ ] デモプロジェクトが本番環境に流用されていないか
- [ ] セッション永続化が必要な場合、`SessionStore`（PostgreSQL/Redis/S3）を使用しているか（v0.2.x+）
- [ ] `TaskUpdatedMessage` でバックグラウンドタスクの終了を正確に検出しているか（v0.2.101+）

### E. 並列実行 (R-055 -- R-060)

- [ ] Git Worktreeで各エージェントに独立した作業コピーを与えているか
- [ ] Cascade Method（計画・実行・統合）の3フェーズを踏んでいるか
- [ ] ターミナルベースで実行しているか（IDE非推奨）
- [ ] `/rename` でインスタンスにラベルを付けて識別しているか
- [ ] サブエージェント委譲でメインコンテキストの負荷を軽減しているか

### F. オーケストレーター (R-061 -- R-068)

- [ ] プロジェクト規模に適したオーケストレーターを選択しているか
- [ ] Command / Agent / Skill の3層階層を理解して使い分けているか
- [ ] `ccg-workflow` ランタイムの初期化を行っているか（`/orchestrate` 使用時）

### G. マルチモデル・トークン管理 (R-069 -- R-077)

- [ ] 用途に応じたモデル選択（sonnet / opus / haiku）をしているか
- [ ] `/compact` を最大50%以前に実行してコンテキスト劣化を防いでいるか
- [ ] タスク切替時に `/clear` でコンテキストをリセットしているか
- [ ] MCPサーバー数が10以下、アクティブツール数が80以下を遵守しているか
- [ ] AgentShieldをCI/CDビルドゲートに組み込んでいるか
